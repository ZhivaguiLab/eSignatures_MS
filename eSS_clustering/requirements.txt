# =============================================================================
# eSignatures Pipeline — Python dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
#
# Python >= 3.9 recommended.
# If using conda, activate your environment first.
# sigProfilerPlotting may require a separate conda/pip step — see its README.

# ---------------------------------------------------------------------------
# Core scientific stack
# ---------------------------------------------------------------------------
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3       # used for cosine similarity utilities

# ---------------------------------------------------------------------------
# Visualisation
# ---------------------------------------------------------------------------
matplotlib>=3.7
seaborn>=0.13

# ---------------------------------------------------------------------------
# Mutational signature plotting
# ---------------------------------------------------------------------------
SigProfilerPlotting>=1.3

# ---------------------------------------------------------------------------
# PDF / image handling
# ---------------------------------------------------------------------------
pdf2image>=1.16         # requires poppler — install via: brew install poppler
                        #   or: conda install -c conda-forge poppler
Pillow>=10.0

# ---------------------------------------------------------------------------
# HTML report generation
# ---------------------------------------------------------------------------
fpdf2>=2.7              # used in report generator for PDF output
plotly>=5.18            # used by generate_interactive_heatmap

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
PyYAML>=6.0               # used for preprocessing.yaml / compound_grouping.yaml
# (argparse, os, re, math, collections, shutil, dataclasses are all stdlib)
